# Build output — regenerated by CI on every push.
node_modules/
_site/

# Sherpa is a separate client site with its own Git repository and its own
# deploy pipeline. It lives here only as a working copy; committing it would
# nest a repo inside this one and drag in ~82MB that does not belong to the
# AlvSolutions site.
Sherpa Site/

# Puppeteer capture scratch — ~900MB of review screenshots. Regenerate with
# shot-bands.mjs / shot-view.mjs rather than storing them.
temporary screenshots/

# A design reference screenshot, not a project asset.
Linear Website SS.png

# Machine-local Claude Code permissions, not shared config.
.claude/settings.local.json

# OS cruft
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
